kind = "web"
previewPath = "/"
title = "AlphaVest"
version = "1.0.0"
id = "artifacts/alphavest"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20464

[services.development]
run = "pnpm --filter @workspace/alphavest run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/alphavest", "run", "build" ]
publicDir = "artifacts/alphavest/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20464"
BASE_PATH = "/"
